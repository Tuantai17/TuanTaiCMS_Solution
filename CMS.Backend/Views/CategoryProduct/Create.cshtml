@*
Sinh Viên: Nguyễn Tuấn Tài
Mã Sinh Viên: 2123110166
Lớp: CCQ2311E
Ngày Tạo: 22/5/2026
Mô tả: Form thêm loại sản phẩm mới, hỗ trợ upload ảnh đại diện.
*@

@model CMS.Data.Entities.CategoryProduct

@{
    ViewData["Title"] = "Thêm loại sản phẩm";
}

<section class="admin-card">
    <div class="admin-card__header">
        <div>
            <p class="admin-eyebrow mb-1">Create Category Product</p>
            <h2 class="h4 fw-bold mb-0"><i class="bi bi-plus-circle"></i> Thêm loại sản phẩm mới</h2>
        </div>
    </div>

    <div class="admin-card__body">
        <form id="category-product-create-form" asp-action="Create" method="post" enctype="multipart/form-data">
            @if (!ViewData.ModelState.IsValid)
            {
                <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
            }

            <div class="row g-4">
                <div class="col-lg-8">
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label fw-bold">
                            Tên loại sản phẩm: <span class="text-danger">*</span>
                        </label>
                        <input asp-for="Name" class="form-control" placeholder="Ví dụ: Điện thoại, Laptop, Phụ kiện..." required />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="ParentId" class="form-label fw-bold">Danh mục cha (Nếu thuộc về danh mục con):</label>
                        <select asp-for="ParentId" class="form-select" asp-items="ViewBag.ParentId">
                            <option value="">-- Chọn danh mục cha (Hoặc để trống nếu là danh mục gốc) --</option>
                        </select>
                        <span asp-validation-for="ParentId" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-bold">Ảnh đại diện:</label>
                        <input type="file" name="uploadImage" class="form-control" accept="image/*" id="imageInput" />
                        <div class="mt-2" id="imagePreviewContainer" style="display: none;">
                            <img id="imagePreview" src="#" alt="Xem trước ảnh" class="rounded border" style="max-width: 200px; max-height: 200px; object-fit: cover;" />
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label fw-bold">Mô tả:</label>
                        <textarea asp-for="Description" class="form-control" rows="5" placeholder="Nhập mô tả ngắn cho loại sản phẩm..."></textarea>
                        <span asp-validation-for="Description" class="text-danger small"></span>
                    </div>

                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn btn-success" id="category-product-save-button">
                            <i class="bi bi-check-circle"></i> Lưu loại sản phẩm
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Quay lại danh sách
                        </a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</section>

@section Scripts {
<script>
  // Xem trước ảnh khi chọn file
  document.getElementById('imageInput').addEventListener('change', function (e) {
    var file = e.target.files[0];
    var container = document.getElementById('imagePreviewContainer');
    var preview = document.getElementById('imagePreview');
    if (file) {
      var reader = new FileReader();
      reader.onload = function (ev) {
        preview.src = ev.target.result;
        container.style.display = 'block';
      };
      reader.readAsDataURL(file);
    } else {
      container.style.display = 'none';
    }
  });

  document.getElementById('category-product-create-form').addEventListener('submit', function (e) {
    if (!confirm('Bạn có chắc muốn thêm loại sản phẩm này không?')) {
      e.preventDefault();
    }
  });
</script>
}
